apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "acme-dynamites"
  description: "Acme dynamites"
  annotations:
    github.com/project-slug: alesbrelih/acme-dynamites
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: acme-resources
spec:
  type: service
  lifecycle: experimental
  owner: "acme-dynamites"
  providesApis:
    - "acme-dynamites-api"
  consumesApis:
    - "localization-api"
